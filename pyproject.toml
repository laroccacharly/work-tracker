[project]
name = "work-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.6",
    "rich>=13.9.4",
]

[project.scripts]
wt = "work_tracker.main:main"
